<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pms.mapper.PmTaskMapper">
  <resultMap id="BaseResultMap" type="com.pms.entity.PmTask">
    <id column="id" property="id"/>
    <result column="project_id" property="projectId"/>
    <result column="parent_id" property="parentId"/>
    <result column="task_code" property="taskCode"/>
    <result column="task_name" property="taskName"/>
    <result column="assignee_id" property="assigneeId"/>
    <result column="assignee_name" property="assigneeName"/>
    <result column="project_name" property="projectName"/>
    <result column="plan_start" property="planStart"/>
    <result column="plan_end" property="planEnd"/>
    <result column="actual_start" property="actualStart"/>
    <result column="actual_end" property="actualEnd"/>
    <result column="progress" property="progress"/>
    <result column="status" property="status"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
  </resultMap>
  <sql id="whereByAssignee">
    <where>
      <if test="assigneeId != null">AND t.assignee_id = #{assigneeId}</if>
    </where>
  </sql>
  <select id="selectPageByAssignee" resultMap="BaseResultMap">
    SELECT t.*, u.real_name AS assignee_name, p.project_name
    FROM pm_task t
    LEFT JOIN sys_user u ON t.assignee_id = u.id
    LEFT JOIN pm_project p ON t.project_id = p.id
    <include refid="whereByAssignee"/>
    ORDER BY t.id DESC LIMIT #{offset}, #{limit}
  </select>
  <select id="countPageByAssignee" resultType="long">
    SELECT COUNT(*) FROM pm_task t <include refid="whereByAssignee"/>
  </select>
  <select id="selectById" resultMap="BaseResultMap">SELECT * FROM pm_task WHERE id = #{id}</select>
  <select id="selectByIdWithNames" resultMap="BaseResultMap">
    SELECT t.*, u.real_name AS assignee_name, p.project_name
    FROM pm_task t
    LEFT JOIN sys_user u ON t.assignee_id = u.id
    LEFT JOIN pm_project p ON t.project_id = p.id
    WHERE t.id = #{id}
  </select>
  <select id="selectByProjectId" resultMap="BaseResultMap">
    SELECT t.*, u.real_name AS assignee_name, p.project_name
    FROM pm_task t
    LEFT JOIN sys_user u ON t.assignee_id = u.id
    LEFT JOIN pm_project p ON t.project_id = p.id
    WHERE t.project_id = #{projectId}
    ORDER BY t.parent_id, t.id
  </select>
  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO pm_task (project_id, parent_id, task_code, task_name, assignee_id, plan_start, plan_end, actual_start, actual_end, progress, status)
    VALUES (#{projectId}, #{parentId}, #{taskCode}, #{taskName}, #{assigneeId}, #{planStart}, #{planEnd}, #{actualStart}, #{actualEnd}, #{progress}, #{status})
  </insert>
  <update id="updateById">
    UPDATE pm_task SET project_id=#{projectId}, parent_id=#{parentId}, task_code=#{taskCode}, task_name=#{taskName}, assignee_id=#{assigneeId}, plan_start=#{planStart}, plan_end=#{planEnd}, actual_start=#{actualStart}, actual_end=#{actualEnd}, progress=#{progress}, status=#{status} WHERE id = #{id}
  </update>
  <delete id="deleteById">DELETE FROM pm_task WHERE id = #{id}</delete>
</mapper>
