<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pms.mapper.PmTodoMapper">
  <resultMap id="BaseResultMap" type="com.pms.entity.PmTodo">
    <id column="id" property="id"/>
    <result column="user_id" property="userId"/>
    <result column="title" property="title"/>
    <result column="biz_type" property="bizType"/>
    <result column="biz_id" property="bizId"/>
    <result column="priority" property="priority"/>
    <result column="due_date" property="dueDate"/>
    <result column="status" property="status"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
  </resultMap>
  <sql id="wherePage">
    <where>
      <if test="userId != null">AND user_id = #{userId}</if>
    </where>
  </sql>
  <select id="selectPage" resultMap="BaseResultMap">
    SELECT * FROM pm_todo <include refid="wherePage"/>
    ORDER BY id DESC LIMIT #{offset}, #{limit}
  </select>
  <select id="countPage" resultType="long">SELECT COUNT(*) FROM pm_todo <include refid="wherePage"/></select>
  <select id="selectById" resultMap="BaseResultMap">SELECT * FROM pm_todo WHERE id = #{id}</select>
  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO pm_todo (user_id, title, biz_type, biz_id, priority, due_date, status)
    VALUES (#{userId}, #{title}, #{bizType}, #{bizId}, #{priority}, #{dueDate}, #{status})
  </insert>
  <update id="updateById">
    UPDATE pm_todo SET user_id=#{userId}, title=#{title}, biz_type=#{bizType}, biz_id=#{bizId}, priority=#{priority}, due_date=#{dueDate}, status=#{status} WHERE id = #{id}
  </update>
  <delete id="deleteById">DELETE FROM pm_todo WHERE id = #{id}</delete>
</mapper>
