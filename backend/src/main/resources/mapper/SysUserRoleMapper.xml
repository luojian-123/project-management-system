<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pms.mapper.SysUserRoleMapper">
  <select id="selectRoleIdsByUserId" resultType="long">
    SELECT role_id FROM sys_user_role WHERE user_id = #{userId}
  </select>
  <select id="selectUserIdsByRoleId" resultType="long">
    SELECT user_id FROM sys_user_role WHERE role_id = #{roleId}
  </select>
  <delete id="deleteByUserId">DELETE FROM sys_user_role WHERE user_id = #{userId}</delete>
  <delete id="deleteByRoleId">DELETE FROM sys_user_role WHERE role_id = #{roleId}</delete>
  <insert id="insert">INSERT INTO sys_user_role (user_id, role_id) VALUES (#{userId}, #{roleId})</insert>
</mapper>

