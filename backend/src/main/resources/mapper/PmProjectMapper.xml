<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pms.mapper.PmProjectMapper">
  <resultMap id="BaseResultMap" type="com.pms.entity.PmProject">
    <id column="id" property="id"/>
    <result column="project_code" property="projectCode"/>
    <result column="project_name" property="projectName"/>
    <result column="description" property="description"/>
    <result column="owner_id" property="ownerId"/>
    <result column="plan_start" property="planStart"/>
    <result column="plan_end" property="planEnd"/>
    <result column="status" property="status"/>
    <result column="created_by" property="createdBy"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
  </resultMap>
  <resultMap id="PageResultMap" type="com.pms.entity.PmProject" extends="BaseResultMap">
    <result column="owner_name" property="ownerName"/>
  </resultMap>
  <sql id="wherePage">
    <where>
      <if test="keyword != null and keyword != ''">AND (project_code LIKE CONCAT('%',#{keyword},'%') OR project_name LIKE CONCAT('%',#{keyword},'%'))</if>
      <if test="status != null and status != ''">AND status = #{status}</if>
    </where>
  </sql>
  <sql id="wherePageP">
    <where>
      <if test="keyword != null and keyword != ''">AND (p.project_code LIKE CONCAT('%',#{keyword},'%') OR p.project_name LIKE CONCAT('%',#{keyword},'%'))</if>
      <if test="status != null and status != ''">AND p.status = #{status}</if>
    </where>
  </sql>
  <select id="selectPage" resultMap="PageResultMap">
    SELECT p.*, u.real_name AS owner_name FROM pm_project p
    LEFT JOIN sys_user u ON p.owner_id = u.id
    <include refid="wherePageP"/>
    ORDER BY p.id DESC LIMIT #{offset}, #{limit}
  </select>
  <select id="countPage" resultType="long">SELECT COUNT(*) FROM pm_project <include refid="wherePage"/></select>
  <select id="selectById" resultMap="BaseResultMap">SELECT * FROM pm_project WHERE id = #{id}</select>
  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO pm_project (project_code, project_name, description, owner_id, plan_start, plan_end, status, created_by)
    VALUES (#{projectCode}, #{projectName}, #{description}, #{ownerId}, #{planStart}, #{planEnd}, #{status}, #{createdBy})
  </insert>
  <update id="updateById">
    UPDATE pm_project SET project_code=#{projectCode}, project_name=#{projectName}, description=#{description}, owner_id=#{ownerId}, plan_start=#{planStart}, plan_end=#{planEnd}, status=#{status} WHERE id = #{id}
  </update>
  <delete id="deleteById">DELETE FROM pm_project WHERE id = #{id}</delete>
</mapper>
