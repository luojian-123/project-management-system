<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pms.mapper.SysRoleMenuMapper">
  <select id="selectMenuIdsByRoleId" resultType="long">
    SELECT menu_id FROM sys_role_menu WHERE role_id = #{roleId}
  </select>
  <delete id="deleteByRoleId">DELETE FROM sys_role_menu WHERE role_id = #{roleId}</delete>
  <delete id="deleteByMenuId">DELETE FROM sys_role_menu WHERE menu_id = #{menuId}</delete>
  <insert id="insert">INSERT INTO sys_role_menu (role_id, menu_id) VALUES (#{roleId}, #{menuId})</insert>
</mapper>

