<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pms.mapper.PmCompanyMapper">
  <resultMap id="BaseResultMap" type="com.pms.entity.PmCompany">
    <id column="id" property="id"/>
    <result column="company_code" property="companyCode"/>
    <result column="company_name" property="companyName"/>
    <result column="sort_order" property="sortOrder"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
  </resultMap>

  <select id="selectList" resultMap="BaseResultMap">
    SELECT * FROM pm_company ORDER BY sort_order ASC, id ASC
  </select>

  <select id="selectById" resultMap="BaseResultMap">
    SELECT * FROM pm_company WHERE id = #{id}
  </select>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO pm_company (company_code, company_name, sort_order)
    VALUES (#{companyCode}, #{companyName}, #{sortOrder})
  </insert>

  <update id="updateById">
    UPDATE pm_company
    SET company_code=#{companyCode}, company_name=#{companyName}, sort_order=#{sortOrder}
    WHERE id = #{id}
  </update>

  <delete id="deleteById">
    DELETE FROM pm_company WHERE id = #{id}
  </delete>
</mapper>

