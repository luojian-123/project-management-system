# 重启服务说明

本目录下提供手动重启**后端**与**前端**服务的 PowerShell 脚本，适用于本地开发或部署目录（如 `D:\PMS系统代码`）下使用。

---

## 脚本说明

| 脚本 | 作用 |
|------|------|
| **deploy-local.ps1** | 本地一键部署：停止旧进程、构建后端、检查前端依赖，并启动前后端 |
| **restart-backend.ps1** | 释放 8080 端口并启动后端（Spring Boot），在新窗口运行 |
| **restart-frontend.ps1** | 释放 5173 端口并启动前端（Vite），在新窗口运行 |
| **restart-all.ps1** | 依次释放 8080、5173，并分别在新窗口启动后端、前端 |
| **生成局域网链接.ps1** | 生成局域网前端/后端访问地址，并复制前端地址到剪贴板 |

---

## 使用方法

### 1. 打开 PowerShell

在**项目根目录**（或 `D:\PMS系统代码`）下打开 PowerShell：

- 方式一：在该目录空白处按住 **Shift + 右键**，选择「在此处打开 PowerShell 窗口」
- 方式二：先 `cd` 到该目录，例如：
  ```powershell
  cd D:\PMS系统代码
  ```

### 2. 执行策略（仅首次如报错时执行）

若运行脚本时报错「无法加载，因为在此系统上禁止运行脚本」，可先执行：

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

再重新运行脚本。

### 3. 运行脚本

**本地一键部署并启动前后端：**

```powershell
.\deploy-local.ps1
```

**只重启后端：**

```powershell
.\restart-backend.ps1
```

**只重启前端：**

```powershell
.\restart-frontend.ps1
```

**同时重启后端和前端：**

```powershell
.\restart-all.ps1
```

脚本会先释放对应端口上的旧进程，再在**新开的 PowerShell 窗口**中启动服务，原窗口可关闭。

---

## 服务地址

| 服务 | 端口 | 地址 |
|------|------|------|
| 后端 API | 8080 | http://localhost:8080/api |
| 前端页面 | 5173 | http://localhost:5173/ |

---

## 注意事项

1. **一键部署脚本** `deploy-local.ps1` 会自动执行后端构建，并在首次无 `node_modules` 时自动执行前端依赖安装。
2. **单独重启后端脚本**：需已编译出 `backend\target\pms-backend-1.0.0.jar`。若没有，请先在 `backend` 目录执行：
   ```powershell
   mvn clean package -DskipTests
   ```
3. **前端**：若不使用 `deploy-local.ps1`，首次使用前请在 `frontend` 目录执行一次 `npm install`。
4. 脚本通过「占用 8080 / 5173 的进程」来结束旧服务，请勿将其他程序固定在这两个端口上。
